# ============================================================================
# yujok Blog · 环境变量模板
# ----------------------------------------------------------------------------
# 使用方法：
#   1. 复制本文件为 .env.local（已被 .gitignore 忽略，绝不会提交）
#   2. 填入真实值
#   3. 运行 `pnpm fetch-weread` 拉取微信读书数据
# ============================================================================

# 微信读书 API Key（格式 wrk-xxxxxxxx），由 weread CLI 颁发
# 千万不要把真实值写在这里！只在 .env.local 中填写
WEREAD_API_KEY=wrk-replace-me

# weread CLI 可执行命令路径（可选，留空则用 PATH 中的 weread）
# 例：C:/Users/yourname/.claude/skills/weread/weread.bat
WEREAD_CLI=
